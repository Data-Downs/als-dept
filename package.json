{
  "name": "agentic-legibility-stack",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "test": "turbo test && vitest run --config vitest.config.ts",
    "clean": "rm -rf apps/citizen-chris/.next apps/citizen-dept/.next apps/legibility-studio/.next",
    "seed": "npx tsx scripts/seed-traces.ts",
    "seed:ledger": "npx tsx scripts/seed-ledger.ts",
    "seed:services": "npx tsx scripts/seed-service-store.ts",
    "push:d1": "npx tsx scripts/push-data-to-d1.ts --all"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.18",
    "turbo": "^2.3.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.18"
  },
  "packageManager": "npm@10.0.0",
  "overrides": {
    "next": "15.1.12"
  }
}
