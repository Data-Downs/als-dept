{
  "name": "@als/agent-tools",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@als/schemas": "*",
    "@als/legibility": "*",
    "@als/service-graph": "*",
    "@als/personal-data": "*",
    "@als/evidence": "*",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
